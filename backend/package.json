{
  "name": "courseapp-backend",
  "version": "1.0.0",
  "description": "Course Registration & Certification backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:frontend": "cd ../frontend && npm install && npm run build",
    "render-build": "npm run build:frontend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^7.8.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "vite": "^5.0.10"
  }
}